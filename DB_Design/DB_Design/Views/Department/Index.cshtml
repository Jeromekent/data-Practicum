@model List<Department>
@{
    ViewData["Title"] = "Home Page";
    //var students = ViewData["Students"] as List<Student>;
    var department = Model;
}

    <script type="text/javascript" src="~/js/Department.js"></script>

<div class=" box-name ui-draggable-handle box ui-draggable ui-droppable">
    <h4>新增部门信息（输入新的部门名称，点击“新增”):</h4>
    <table>
        <thead>
            <tr>
                <th>部门名称</th>
                <th>&nbsp</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><input id="newName" type="text" style="height:25px;width:100px" /></td>
                <th></th>
                <td><button type="button" onclick="Add()">新增</button></td>
            </tr>
        </tbody>
    </table>
</div>

<br />

<div class=" box-name ui-draggable-handle">
    <span>出差信息修改：修改相应记录后，点击“修改”按钮</span>
    <br />
    <span>出差信息删除：对要删除的学生记录，点击“删除”按钮</span>
    <br /> <br />
</div>

<div class="box-content no-padding">
    <table class="table table-striped table-bordered table-hover table-heading no-border-bottom">
        <thead>
            <tr>
                <th>序号</th>
                <th>部门编号</th>
                <th>部门名称</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0, j = 1; i < department.Count; i++, j++)
            {
            <tr>
                <td>@j</td>
                <td>@department[i].DId</td>
                <td><input id="DName_@department[i].DId" type="text" value="@department[i].DName" style="height:25px;width:150px" /></td>
                <td>
                    <button class="btn btn-primary" type="button" onclick="Update(@department[i].DId)">修改</button>
                    <button class="btn btn-danger" type="button" onclick="Delete(@department[i].DId);">删除</button>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>